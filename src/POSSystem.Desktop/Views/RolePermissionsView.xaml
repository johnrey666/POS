<UserControl x:Class="POSSystem.Desktop.Views.RolePermissionsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Margin="40">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,24">
            <TextBlock Text="Role Permissions" Style="{StaticResource Heading2}" Margin="0,0,0,4"/>
            <TextBlock Text="Select a role and configure what it can access"
                       Style="{StaticResource BodyMedium}"/>
        </StackPanel>

        <!-- Role Selector -->
        <Grid Grid.Row="1" Margin="0,0,0,24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="16"/>
                <ColumnDefinition Width="240"/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="Role" VerticalAlignment="Center" FontWeight="SemiBold"
                       FontSize="14" Foreground="{DynamicResource Gray700Brush}"/>
            
            <ComboBox Grid.Column="2"
                      ItemsSource="{Binding Roles}"
                      SelectedItem="{Binding SelectedRole}"
                      DisplayMemberPath="Name"
                      Height="44"
                      Style="{StaticResource ModernComboBox}"
                      FontSize="14">
                <ComboBox.Resources>
                    <Style TargetType="ComboBoxItem">
                        <Setter Property="Background" Value="White"/>
                        <Setter Property="Foreground" Value="{DynamicResource Gray900Brush}"/>
                        <Setter Property="Padding" Value="14,10"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{DynamicResource Gray50Brush}"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="{DynamicResource PrimaryLightBrush}"/>
                                <Setter Property="Foreground" Value="{DynamicResource PrimaryBrush}"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </ComboBox.Resources>
            </ComboBox>
        </Grid>

        <!-- Permissions Table -->
        <Border Grid.Row="2" Style="{StaticResource CardLarge}" Padding="0">
            <DataGrid ItemsSource="{Binding Permissions}"
                      Style="{StaticResource ModernDataGrid}"
                      ColumnHeaderStyle="{StaticResource ModernColumnHeader}"
                      RowStyle="{StaticResource ModernRow}"
                      Background="Transparent">
                <DataGrid.Columns>
                    <DataGridCheckBoxColumn Header="Enabled"
                                            Binding="{Binding IsEnabled, UpdateSourceTrigger=PropertyChanged}"
                                            Width="100">
                        <DataGridCheckBoxColumn.ElementStyle>
                            <Style TargetType="CheckBox">
                                <Setter Property="HorizontalAlignment" Value="Center"/>
                                <Setter Property="VerticalAlignment" Value="Center"/>
                                <Setter Property="Foreground" Value="{DynamicResource Gray900Brush}"/>
                            </Style>
                        </DataGridCheckBoxColumn.ElementStyle>
                    </DataGridCheckBoxColumn>
                    <DataGridTextColumn Header="Permission" Binding="{Binding Name}" Width="240" IsReadOnly="True"/>
                    <DataGridTextColumn Header="Code" Binding="{Binding Code}" Width="180" IsReadOnly="True"/>
                    <DataGridTextColumn Header="Category" Binding="{Binding Category}" Width="*" IsReadOnly="True"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <!-- Actions -->
        <StackPanel Grid.Row="3" Margin="0,20,0,0">
            <TextBlock Text="{Binding ErrorMessage}" Foreground="{DynamicResource DangerBrush}" 
                       TextWrapping="Wrap" FontSize="13" Margin="0,0,0,4"/>
            <TextBlock Text="{Binding StatusMessage}" Foreground="{DynamicResource SuccessBrush}" 
                       TextWrapping="Wrap" FontSize="13" Margin="0,0,0,16"/>
            <Button Content="Save Permissions"
                    Style="{StaticResource PrimaryButton}"
                    Command="{Binding SaveCommand}"
                    HorizontalAlignment="Left"
                    Height="44"
                    Padding="24,0"/>
        </StackPanel>
    </Grid>
</UserControl>
